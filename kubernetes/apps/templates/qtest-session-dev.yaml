apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: qtest-session-dev
  namespace: argocd
  {{- with .Values.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: {{ .Values.applications.qtestsessiondev.namespace }}
    server: {{ .Values.clusters.stage }}
  project: {{ .Values.project }}
  source:
    chart: {{ .Values.applications.qtestsessiondev.chart }}
    repoURL: {{ .Values.qtest.chart }}
    targetRevision: "*"
    helm:
      values: |
        service:
          type: ClusterIP
          port: 8080
          targetport: 8080
        ingress:
          enabled: true
          annotations:
            kubernetes.io/ingress.class: alb
            alb.ingress.kubernetes.io/scheme: internet-facing
            alb.ingress.kubernetes.io/target-type: ip
            alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:us-east-1:242129367204:certificate/7267f31c-d515-401a-b61b-d1780879b763
            alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS":443}, {"HTTP":80}]'
            alb.ingress.kubernetes.io/group.name: qtest-dev
          labels: {}
          hosts:
            - session.container-dev.qtestdev.com
          paths:
            - /*
        qTestSession:
          qTestSessionJSHome: /usr/local/sessions-js
          qTestSessionNodeEnvironment: production
          qTestSessionPort: "8080"
          qTestSessionDBConnection: postgresql://postgres:RW9dSqzC6!d!7!ED@qtestproduction.ckial7bswutk.us-east-1.rds.amazonaws.com/sessions
          qTestManagerHost: https://qtestdev1.container-dev.qtestdev.com
          qTestSessionMasterToken: Qtoy
          qTestSessionStorageType: disk_storage
          qTestSessionStorageRootPath: /data
          qTestSessionSecretKey: Q3AyVEJYQ0lHb3N3dWs1eklwTG1nQm1QRkxWMDVrU1RKRHBWMXNaM3Y4VVBoWFBZMERzaERVdmwwOW1seVdETw==
        autoscaling:
          enabled: false